name: Type-check

on:
  push:
    branches: [main. dev]
  pull_request:
    branches: [main, dev]

jobs:
  action:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - run: yarn install
      - run: yarn type-check
